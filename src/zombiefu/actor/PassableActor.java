package zombiefu.actor;

import jade.core.Actor;
import jade.util.datatype.ColoredChar;

public abstract class PassableActor extends Actor {

    public PassableActor(ColoredChar face) {
        super(face);
    }

}
